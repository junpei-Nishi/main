module.exports = function(RED) {
    function PythonShellNode(config) {
        RED.nodes.createNode(this,config);
        this.name = config.name;
        this.python = config.python;
        this.pypath = config.pypath;
        this.pyshellpath = config.pyshellpath;
				this.dataname = config.dataname;
				this.cameraid = config.cameraid;
				this.size = config.size;
				this.cameraguide = config.cameraguide;
				this.mirror = config.mirror;

        var node = this;
        node.on("input", function(msg) {
            try {
                var {PythonShell} = require(node.pyshellpath); 
								var argsarray = [];
								if(node.dataname){
										argsarray.push("-n",node.dataname);
								}
								if(node.cameraid){
										argsarray.push("-id",node.cameraid);
								}
								if(node.size){
										argsarray.push("-s",node.size);
								}
								argsarray.push("-g"); 
								if(node.mirror){
										argsarray.push("-m");
								}
                let options = {
                    pythonPath: node.pypath, 
                    pythonOptions: ['-u'],
										scriptPath: '/Users/nishijunpei/Desktop/SDTest-Commercial-CUI-master',
										args: argsarray,
                };

                PythonShell.run(node.python, options, function (err) {
                    if (err) {
                        msg.payload = err;
                    } else {
                    		msg.payload = "finished";
                    }
                    node.send(msg);
                });
            } catch(err) {
                node.error(err.message);
            }
        });
    }
    RED.nodes.registerType("python-shell",PythonShellNode);
}
